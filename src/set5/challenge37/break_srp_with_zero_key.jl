# Not sure whether I am supposed to code it. It is obvious that having the client send N * 0, N * 1,
# ..., N * k as public keys will make the server compute the secret as 0.
#
# So I just skip this one.
